# example.config.yaml — annotated template for EVE Data Framework
# Generated by: python build.py --example-config
# Copy this file to config.yaml and adjust as needed.
# config.yaml is gitignored and must NEVER be committed.

Runtime:
  host: "127.0.0.1"                # bind address for the Flask web server
                                   # use "0.0.0.0" to listen on all network interfaces
  port: 5000                       # TCP port for the Flask web server
  # debug: false                   # enable Flask debug mode (extra logging, auto-reload)
  # trace_esi: false               # print every ESI HTTP request to the console
  # auto_install: false            # pip-install missing requirements automatically
  # secret_key: "change-me"        # Flask session secret (use a long random string in prod)
  # transport: "http"              # "http" or "https" — set to "https" when behind a TLS-terminating proxy
  # github_repo: ""                # GitHub repository URL for system updates (default: NolieRavioli/eve_data_framework3)

# Bootstrap controls which subsystems auto-update when the server starts.
# Set either flag to false to skip auto-update on boot (useful for production
# deployments that pin generated code to git and update manually).
# Changes take effect on the next server restart.
Bootstrap:
  auto_update_sde: true   # download + rebuild SDE warehouse if stale
  auto_update_esi: true   # fetch latest ESI spec + regenerate codegen if stale

Python Console:
  global_log_level: INFO          # log level for the terminal / console output
  werkzeug_log_level: INFO        # log level for the Flask/Werkzeug request logger
  # ── Per-logger overrides ─────────────────────────────────────────────────
  # Uncomment and adjust any logger below to tune its verbosity independently.
  # Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
  # werkzeug: INFO
  # flask: INFO
  # duckdb: INFO
  # urllib3: INFO
  # requests: INFO
  #
  # analysis.affiliation_sync: INFO
  # analysis.alliance_enrichment: INFO
  # analysis.asset_enrichment: INFO
  # analysis.corporation_discovery: INFO
  # analysis.freelance_enrichment: INFO
  # analysis.killmail_enrichment: INFO
  # analysis.market_browser: INFO
  # analysis.public_contract_enrichment: INFO
  # analysis.war_enrichment: INFO
  # applications: INFO
  # applications.admin_panel.routes: INFO
  # applications.dashboard.routes: INFO
  # applications.db_viewer.routes: INFO
  # applications.market_browser.routes: INFO
  # applications.sde_browser.routes: INFO
  # applications.system.routes: INFO
  # applications.task_viewer.routes: INFO
  # collectors.alliance: INFO
  # collectors.alliance.contacts: INFO
  # collectors.character.collectors: INFO
  # collectors.character.combat: INFO
  # collectors.character.comms: INFO
  # collectors.character.extended: INFO
  # collectors.character.finance: INFO
  # collectors.character.identity: INFO
  # collectors.character.industry: INFO
  # collectors.character.presence: INFO
  # collectors.character.skillqueue: INFO
  # collectors.character.social: INFO
  # collectors.corp: INFO
  # collectors.corp.assets: INFO
  # collectors.corp.contacts: INFO
  # collectors.corp.contracts: INFO
  # collectors.corp.industry: INFO
  # collectors.corp.infrastructure: INFO
  # collectors.corp.market: INFO
  # collectors.corp.members: INFO
  # collectors.corp.misc: INFO
  # collectors.corp.org: INFO
  # collectors.corp.stats: INFO
  # collectors.corp.wallet: INFO
  # collectors.market.history: INFO
  # collectors.market.regions: INFO
  # collectors.market.structures: INFO
  # collectors.public_data.alliances: INFO
  # collectors.public_data.contracts: INFO
  # collectors.public_data.fw: INFO
  # collectors.public_data.industry: INFO
  # collectors.public_data.loyalty: INFO
  # collectors.public_data.market: INFO
  # collectors.public_data.sovereignty: INFO
  # collectors.public_data.structures: INFO
  # collectors.public_data.universe_extras: INFO
  # collectors.structures.discover: INFO
  # core.auth.credentials: INFO
  # core.auth.identity: INFO
  # core.auth.sso: INFO
  # core.auth.tokens: INFO
  # core.bus.handler: INFO
  # core.bus.process_pub: INFO
  # core.bus.websocket: INFO
  # core.config: INFO
  # core.io: INFO
  # core.io.encryption: INFO
  # core.io.entity_db: INFO
  # core.io.market_buffer: INFO
  # core.io.public: INFO
  # core.io.reader: INFO
  # core.io.sde: INFO
  # core.io.sde_loader: INFO
  # core.io.stats: INFO
  # core.io.writer: INFO
  # core.esi.cache: INFO
  # core.esi.corp.assets: INFO
  # core.esi.corp.contacts: INFO
  # core.esi.corp.contracts: INFO
  # core.esi.corp.corporation: INFO
  # core.esi.corp.corporation_projects: INFO
  # core.esi.corp.faction_warfare: INFO
  # core.esi.corp.freelance_jobs: INFO
  # core.esi.corp.industry: INFO
  # core.esi.corp.killmails: INFO
  # core.esi.corp.loyalty: INFO
  # core.esi.corp.market: INFO
  # core.esi.corp.planetary_interaction: INFO
  # core.esi.corp.wallet: INFO
  # core.esi.personal.assets: INFO
  # core.esi.personal.calendar: INFO
  # core.esi.personal.character: INFO
  # core.esi.personal.clones: INFO
  # core.esi.personal.contacts: INFO
  # core.esi.personal.contracts: INFO
  # core.esi.personal.corporation_projects: INFO
  # core.esi.personal.faction_warfare: INFO
  # core.esi.personal.fittings: INFO
  # core.esi.personal.fleets: INFO
  # core.esi.personal.freelance_jobs: INFO
  # core.esi.personal.industry: INFO
  # core.esi.personal.killmails: INFO
  # core.esi.personal.location: INFO
  # core.esi.personal.loyalty: INFO
  # core.esi.personal.mail: INFO
  # core.esi.personal.market: INFO
  # core.esi.personal.planetary_interaction: INFO
  # core.esi.personal.search: INFO
  # core.esi.personal.skills: INFO
  # core.esi.personal.universe: INFO
  # core.esi.personal.user_interface: INFO
  # core.esi.personal.wallet: INFO
  # core.esi.public.alliance: INFO
  # core.esi.public.character: INFO
  # core.esi.public.contracts: INFO
  # core.esi.public.corporation: INFO
  # core.esi.public.dogma: INFO
  # core.esi.public.faction_warfare: INFO
  # core.esi.public.freelance_jobs: INFO
  # core.esi.public.incursions: INFO
  # core.esi.public.industry: INFO
  # core.esi.public.insurance: INFO
  # core.esi.public.killmails: INFO
  # core.esi.public.market: INFO
  # core.esi.public.meta: INFO
  # core.esi.public.planetary_interaction: INFO
  # core.esi.public.routes: INFO
  # core.esi.public.sovereignty: INFO
  # core.esi.public.status: INFO
  # core.esi.public.universe: INFO
  # core.esi.public.wars: INFO
  # core.esi.rate: INFO
  # core.esi.registry: INFO
  # core.system.bootstrap: INFO
  # core.system.lifecycle: INFO
  # core.system.updater: INFO
  # core.tasks.engine: INFO
  # core.tasks.jobs: INFO
  # core.tasks.persist: INFO
  # core.web: INFO
  # main: INFO

# SDE section toggles — set false to skip loading a dataset at startup.
# Disabled datasets lazy-load on first use (or return defaults if too expensive).
SDE:
  database_file: "_publicData/public.duckdb"
  load_types: true            # type ID <-> name maps          (~26 MB, ~4s)
  load_groups: true           # item group hierarchy            (~0.3 MB, fast)
  load_categories: true       # item categories                 (~0.1 MB, fast)
  load_market_groups: true    # market group tree               (~0.3 MB, fast)
  load_universe: true         # solar-system -> region map      (8,400+ files, ~15s)
  load_blueprints: false       # manufacturing blueprints        (~4 MB, ~8s)
  load_type_materials: false   # reprocessing materials          (~2 MB, ~3s)
  load_type_dogma: false       # item dogma attributes           (~26 MB, ~60s+)

Environment Variables:
  LANGUAGE: "en"
  SUPPORTED_LANGUAGES: "en"
  PUBLIC_DATA_FOLDER: "_publicData"
  EVE_PRIVATE_DATABASE_FOLDER: "_privateData/"
  SDE_PATH: "_sde/"

# Role-based access control.
# default_roles are granted to every new user on their first login
# (site owner is exempt — they have full access already).
# Role names must match the required_role field in each application's ToolManifest.
Auth:
  default_roles:
    - dashboard
    - database
    - sde
    - tasks

Structures:
  forbidden_cooldown_days: 21       # days to skip transiently-inaccessible structures (404/network)
  unauthorized_cooldown_days: 7     # days to skip 403-inaccessible structures during enrichment
  authorized_cooldown_seconds: 3600 # seconds to skip structures that succeed

Market:
  unauthorized_cooldown_days: 7         # days to skip structures that return 403 on market endpoint
  forbidden_cooldown_days: 21           # days to skip structures that fail for other reasons
  authorized_cooldown_seconds: 3600     # seconds to skip structures that succeed on market endpoint
  max_retries: 3                        # per-page fetch retry limit before skipping a structure
  rate_limit_sleep_seconds: 10          # sleep duration when ESI returns 420 (error rate limit)
